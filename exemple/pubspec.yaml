name: flutter_koin_example
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_koin:
    path: ..

dev_dependencies:
  build_runner: ^2.4.15

flutter:
  uses-material-design: true